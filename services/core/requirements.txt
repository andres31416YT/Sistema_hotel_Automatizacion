# Core service requirements
# This service uses asyncpg for PostgreSQL database connections, redis-py for Redis, and python-dotenv for environment variables.
# It also uses asyncio (built-in) and logging (built-in).

asyncpg==0.29.0
redis==5.0.5
python-dotenv==1.0.1