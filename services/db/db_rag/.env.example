# RAG PostgreSQL DB with PGVector Extension Environment Variables Example
# Copy this to .env and fill in your actual values
# Do not commit .env to git

POSTGRES_USER=rag_user
POSTGRES_PASSWORD=rag_password
POSTGRES_DB=rag_db